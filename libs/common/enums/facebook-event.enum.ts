export enum FacebookTopEvent {
  AdView = 'ad.view',
  PageLike = 'page.like',
  Comment = 'comment',
  VideoView = 'video.view',
}

export enum FacebookBottomEvent {
  AdClick = 'ad.click',
  FormSubmission = 'form.submission',
  CheckoutComplete = 'checkout.complete',
}

export enum FacebookReferrer {
  Newsfeed = 'newsfeed',
  Marketplace = 'marketplace',
  Groups = 'groups',
}

export enum Gender {
  Male = 'male',
  Female = 'female',
  NonBinary = 'non-binary',
}

export enum ClickPosition {
  TopLeft = 'top_left',
  BottomRight = 'bottom_right',
  Center = 'center',
}

export enum Device {
  Mobile = 'mobile',
  Desktop = 'desktop',
}

export enum Browser {
  Chrome = 'Chrome',
  Firefox = 'Firefox',
  Safari = 'Safari',
}
